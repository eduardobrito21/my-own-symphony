{
  "name": "@symphony/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types between the Symphony daemon and the dashboard",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && rm -rf dist"
  }
}

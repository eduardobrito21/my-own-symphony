# FakeTracker fixture for local development.
#
# Each issue is a snapshot — the same shape the LinearTracker (Plan 06)
# will produce after normalization. The FakeTracker filters this list
# by `tracker.active_states` from the workflow on each `fetchCandidateIssues`
# call.

issues:
  - id: fake-1
    identifier: SYMP-1
    title: Wire up the polling loop
    description: First task to demonstrate dispatch.
    priority: 1
    state: Todo
    labels: [demo]
    blocked_by: []
    created_at: '2026-04-28T10:00:00Z'

  - id: fake-2
    identifier: SYMP-2
    title: Add a snapshot endpoint
    description: HTTP API will consume this.
    priority: 2
    state: In Progress
    labels: [demo, observability]
    blocked_by: []
    created_at: '2026-04-28T10:05:00Z'

  - id: fake-3
    identifier: SYMP-3
    title: Investigate flaky CI
    description: Blocked while we wait for SYMP-1 to complete.
    priority: 3
    state: Todo
    labels: [demo, ci]
    blocked_by:
      - id: fake-1
        identifier: SYMP-1
        state: Todo # non-terminal: SYMP-3 is ineligible until SYMP-1 finishes

  - id: fake-4
    identifier: SYMP-4
    title: Closed task
    description: Should never be dispatched.
    state: Done
    labels: [demo]
    created_at: '2026-04-25T10:00:00Z'
